# 🌸 Mochimo Haiku Game — *The Verse Keepers*

> *In the beginning, there was a single Haiku...*

A game built on the [Mochimo](https://mochimo.org) post-quantum blockchain — the only
network in the world whose proof-of-work solutions are **poems**. Every solved Mochimo
block carries a grammatically valid haiku as its nonce. This game turns that quiet magic
into play, and rewards players in **nanoMochi (nMCM)** on the real Mochimo mainnet.

Built to drive adoption of Mochimo, the first live quantum-resistant cryptocurrency
(mainnet since June 2018, secured by WOTS+ hash-based signatures).

## The Narrative

In a near future, quantum computers have shattered the cryptography holding the world's
ledgers together. One network survives — a chain that does not write its history in
numbers, but in *verse*. Every ~2 minutes it births a new haiku as proof the world still
beats. Over a quintillion poems and counting.

Players are **Verse Keepers**: poets of the surviving chain. In the **Haiku Forge**, they
compose haiku from the very same 256-word semantic dictionary the Mochimo network itself
uses — meaning every player-forged haiku that passes the Forge **is a structurally valid
Mochimo mining nonce**, judged by the exact grammar rules in the network's consensus code.

Time in the game follows the chain itself:

| Chain concept        | In-game meaning                                        |
| -------------------- | ------------------------------------------------------ |
| 1 block (~2 min)     | One heartbeat; a new *Haiku of the Block* (real, live) |
| 1 Aeon (256 blocks)  | One season (~6h): anthology closes, rewards paid       |
| Neogenesis block     | The world reborn; season rotates (4 Aeons ≈ 1 day)     |
| Pseudoblock          | "The network is silent — no verse this beat"           |

Each Aeon, players submit haiku to the **Anthology of the Aeon** and vote for their
favorites. Top verses earn nanoMochi, paid on mainnet in a **single multi-destination
transaction** (Mochimo supports up to 256 recipients per TX — one payout TX per Aeon,
with an on-chain memo like `AEON-12345-RANK-001` so every reward is publicly verifiable).

## Repository Layout

```
codec/    The Haiku Codec — heart of the project (Milestone 1, DONE)
  haiku.js                 decode nonce→haiku, grammar validation, compose (Forge)
  trigg_data.json          256-word dictionary + 10 grammar frames (machine-extracted)
  haiku.test.js            test suite incl. golden vectors
  test_vectors*.txt        1,000 golden vectors generated by the reference C code
scripts/  extract_dict.py  extracts dictionary/frames from mochimo src/trigg.c
docs/     game design documents
server/   (Milestone 2) Mesh API poller, rounds, anthology, voting
web/      (Milestone 3) PWA frontend — mint-green on charcoal, per the Mochimo mark
```

## The Haiku Codec — verified against consensus code

The codec is a faithful JavaScript port of the haiku parts of **Trigg's Algorithm**
(`src/trigg.c` in [mochimodev/mochimo](https://github.com/mochimodev/mochimo)):

- `decodeNonce(hexOrBytes)` — a Mochimo nonce is 32 bytes = two 16-byte frames; each byte
  indexes the 256-word dictionary (`trigg_expand`).
- `syntaxOk(frame16)` — validates a frame against the 10 semantic grammar frames — the
  same check the network runs on every block (`trigg_syntax`).
- `composeFrame / composeNonce` — the Forge path: player words → bytes → grammar check.
- `wordBank()` — the full word list with semantic features, for the Forge UI.

**Verification:** the dictionary and grammar are machine-extracted from the C source (no
hand copying), and the port is differential-tested against the *compiled reference
implementation*: 5,000 generated nonces + 5,000 random garbage frames, **0 mismatches**
in both expansion text and grammar verdicts. 1,000 of those vectors ship in this repo as
regression fixtures.

```bash
node codec/haiku.test.js   # runs the full suite
```

Example output of the codec on a generated nonce:

```
morning hail
at a frozen bon fire--
sick

in ice
drowned cranes
entering
```

## Wallet Login & Rewards (design)

Mochimo uses WOTS+ **one-time** signatures — signing a login challenge would burn a key.
So login is designed around what is stable and cheap on Mochimo:

1. Player identifies with their **account tag** (the 20-byte number that never changes).
2. Ownership is proven once via a micro-transaction to the game address carrying a unique
   **memo code** (Mochimo's structured reference field, e.g. `HAIKU-4F2A-91`), detected
   through the [Mesh API](https://github.com/NickP005/mochimo-mesh).
3. Guest mode requires no wallet at all — a wallet is only needed to claim rewards.

## Roadmap

- [x] **M1 — Haiku Codec**: extraction, decode, grammar, compose, golden-vector tests
- [ ] **M2 — Server**: Mesh API poller (skip pseudoblocks/neogenesis), Aeon clock,
      anthology + voting API, tag-login via memo micro-TX
- [ ] **M3 — Web PWA**: Haiku of the Block (live), the Forge, Anthology, Aeon seasons
- [ ] **M4 — Rewards**: multi-destination payout tooling, on-chain verifiable receipts

## Credits & License

- Haiku dictionary, grammar frames, and Trigg's Algorithm are from the
  [Mochimo reference implementation](https://github.com/mochimodev/mochimo)
  © Adequate Systems LLC — see their license for terms covering derived network code.
- Game code in this repository: MIT (see `LICENSE`).
